// //Author Maxim Kuzmin//makc//

/** Ядро. Основа. Внедрение зависимостей. Токен. Локальное хранилище. */
export const appBaseDiTokenLocalStorage = 'appBaseDiTokenLocalStorage';

/** Ядро. Основа. Внедрение зависимостей. Токен. Сессионное хранилище. */
export const appBaseDiTokenSessionStorage = 'appBaseDiTokenSessionStorage';

/** Ядро. Основа. Внедрение зависимостей. Токен. Окно. */
export const appBaseDiTokenWindow = 'appBaseDiTokenWindow';
