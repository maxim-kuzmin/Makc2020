// //Author Maxim Kuzmin//makc//

import {AppHostCalendarState} from '@app/host/calendar/host-calendar-state';
import {AppHostCalendarLocale} from '@app-skin/host/calendar/host-calendar-locale';

/** Хост. Календарь. Состояние. */
export class AppSkinHostCalendarState extends AppHostCalendarState {

  /**
   * Локаль.
   * @type {AppHostCalendarLocale}
   */
  locale: AppHostCalendarLocale;
}
