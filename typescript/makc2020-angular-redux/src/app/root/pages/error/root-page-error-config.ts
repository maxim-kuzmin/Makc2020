// //Author Maxim Kuzmin//makc//

import {appRootConfigFullPath} from '@app/root/root-config';

/** Корень. Страницы. Ошибка. Конфигурация. Путь. */
const appRootPageErrorConfigPath = 'error';

/** Корень. Страницы. Ошибка. Конфигурация. Маршрут. Путь. */
export const appRootPageErrorConfigRoutePath = appRootPageErrorConfigPath;

/** Корень. Страницы. Ошибка. Конфигурация. Полный путь. */
export const appRootPageErrorConfigFullPath =
  `${appRootConfigFullPath}/${appRootPageErrorConfigPath}`;

/** Корень. Страницы. Ошибка. Конфигурация. Ключ. */
export const appRootPageErrorConfigKey = 'RootPageError';

/** Корень. Страницы. Ошибка. 404. Конфигурация. Ключ. */
export const appRootPageError404ConfigKey = 'RootPageError404';
