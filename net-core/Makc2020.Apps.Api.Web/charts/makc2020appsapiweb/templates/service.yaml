apiVersion: v1
kind: Service
metadata:
  name: {{ template "makc2020appsapiweb.fullname" . }}
  labels:
    app: {{ template "makc2020appsapiweb.name" . }}
    chart: {{ template "makc2020appsapiweb.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "makc2020appsapiweb.name" . }}
    release: {{ .Release.Name }}
